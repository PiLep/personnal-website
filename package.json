{
  "name": "pierrelepetit.com",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "mkdir -p dist && tailwindcss -i ./src/input.css -o ./dist/tailwind.css --watch",
    "build": "mkdir -p dist && NODE_ENV=production tailwindcss -i ./src/input.css -o ./dist/tailwind.css --minify",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
