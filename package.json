{
  "name": "pierrelepetit.com",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "mkdir -p dist && tailwindcss -i ./src/input.css -o ./dist/tailwind.css --watch",
    "build": "mkdir -p dist && tailwindcss -i ./src/input.css -o ./dist/tailwind.css --minify",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17"
  }
}
